// functional interface for generating OTP
// functional interface is an interface that has only one abstract method. 
// It can have multiple default and static methods, but only one abstract method. 
// Functional interfaces are used as the basis for lambda expressions in Java.

public interface otpinterface {
    int generateOtp();
}

